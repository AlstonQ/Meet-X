# Local Docker Infrastructure

`docker-compose.yml` starts:

- Postgres 16 with pgvector
- Redis
- MinIO

Production infrastructure will live under `infra/terraform` and must support EU launch residency before customer content is processed.
