{
  "name": "@meet-x/desktop-agent",
  "private": true,
  "version": "0.1.0",
  "description": "Meet-X Windows desktop recorder for visible system-audio and microphone capture.",
  "main": "main.cjs",
  "scripts": {
    "dev": "electron .",
    "build": "node --check main.cjs && node --check preload.cjs && node --check renderer/renderer.js",
    "lint": "node --check main.cjs && node --check preload.cjs && node --check services/detection-service.cjs && node --check renderer/renderer.js",
    "typecheck": "node --check main.cjs && node --check preload.cjs && node --check services/detection-service.cjs && node --check renderer/renderer.js",
    "test": "node --test tests/*.test.cjs"
  },
  "devDependencies": {
    "electron": "33.2.1"
  }
}