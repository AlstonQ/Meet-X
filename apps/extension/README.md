# Chrome Extension

Chrome MV3 capture is required for launch, but implementation is deferred to Phase 1.

The extension must:

- disclose recording clearly;
- capture tab audio/video where browser APIs allow;
- upload resumably;
- stop capture on participant opt-out where policy requires;
- never provide silent or invisible recording.
